//step-1 npm init -y Next step----> in packages.json add --->this "type":"module"

import os from "os";

console.log(os.freemem());
console.log(os.arch());
console.log(os.homedir());
console.log(os.type());
console.log(os.platform());
